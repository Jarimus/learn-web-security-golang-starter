PAWPAL_API_KEY=replace_with_your_pawpal_api_key (create random for tests openssl rand -hex 32)
DOWNLOAD_SIGNING_KEY=replace_with_64_hex_characters
APP_ORIGIN=http://localhost:3030
PORT=3030
ATTACKER_LAB_PORT=4040
DATA_ENCRYPTION_ACTIVE_VERSION=current_version
DATA_ENCRYPTION_KEY_V1=v1_key_here

# Set to 1 only when every request passes through exactly one trusted proxy
TRUST_PROXY_HOPS=0